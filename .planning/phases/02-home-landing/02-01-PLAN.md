---
phase: 02-home-landing
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/tokens.css
autonomous: true
requirements:
  - REQ-H01
  - REQ-H02
  - REQ-H03
  - REQ-H04
  - REQ-H05
  - REQ-H06
  - REQ-N02

must_haves:
  truths:
    - "Philosophy statement ('I design game systems…') is in the DOM above the name/role, inside the hero section"
    - "Designer's name ('Rebecca Anisman') and role ('Narrative & Experience Engineer') appear in the hero"
    - "Three project cards exist in the HTML, each wrapped in an <a> link pointing to a unique /work/slug/ path"
    - "The inline SVG grain filter element is present immediately after the opening <body> tag"
    - "No page content is gated behind animation — hero text is in static HTML, not injected by JS"
    - "The --nav-height custom property is defined in tokens.css"
  artifacts:
    - path: "index.html"
      provides: "Home page with hero + cards markup"
      contains: "hero__statement, hero__identity, cards-grid, project-card"
    - path: "css/tokens.css"
      provides: "--nav-height token for hero height calculation"
      contains: "--nav-height"
  key_links:
    - from: "index.html (.hero)"
      to: "css/tokens.css (--nav-height)"
      via: "CSS calc() in components.css references this token"
      pattern: "--nav-height"
    - from: "index.html (.project-card)"
      to: "/work/[slug]/"
      via: "href attribute on each card <a> wrapper"
      pattern: "href=\"/work/"
---

<objective>
Replace the Phase 1 placeholder skeleton in index.html with the complete home page markup: hidden SVG grain filter, hero section (philosophy statement + name/role), and three project cards section. Add --nav-height token to tokens.css.

Purpose: Establishes the semantic HTML structure that Phase 2 Plan 02 (CSS styles) and later content phases depend on. This is the structural foundation of the home page.
Output: A semantically complete index.html with all required content in place; --nav-height token available for hero height calculation.
</objective>

<execution_context>
@C:/Users/rebec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rebec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-home-landing/02-CONTEXT.md
@.planning/phases/02-home-landing/02-RESEARCH.md
@css/tokens.css
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --nav-height token to tokens.css</name>
  <files>css/tokens.css</files>
  <action>
    In the `/* ── Layout ──────────────────────────────────────── */` section of `css/tokens.css`, add the `--nav-height` custom property after the existing layout tokens:

    ```css
    --nav-height: 72px;  /* sticky nav height: space-4×2 padding + ~text-lg line-height */
    ```

    Add it on the line immediately after `--max-width-narrow`. Do not change any other token. The value 72px = 16px top padding + 16px bottom padding + ~28px logo text at --text-lg (~1.777rem) × 1.4 line-height. This token is used in components.css for the hero min-height calc.
  </action>
  <verify>
    <automated>grep --color "nav-height" "C:/Users/rebec/Documents/Portfolio/css/tokens.css"</automated>
    <manual>Confirm the line reads: --nav-height: 72px; and is inside the :root block in the Layout section</manual>
    <sampling_rate>after this task, before Task 2</sampling_rate>
  </verify>
  <done>css/tokens.css contains --nav-height: 72px inside :root</done>
</task>

<task type="auto">
  <name>Task 2: Replace index.html placeholder with hero + cards markup</name>
  <files>index.html</files>
  <action>
    Replace the entire `<body>` content of `index.html` with the structure below. Keep the `<head>` exactly as-is — do not change anything in `<head>`. The body structure is:

    1. **Hidden SVG grain filter** — place immediately after `<body class="page-home">` open tag, before `#site-nav`. Use `width:0; height:0; position:absolute; overflow:hidden` (NOT `display:none` — display:none prevents browser from processing SVG filters). Filter id must be `grain`. feTurbulence parameters: `type="fractalNoise" baseFrequency="0.72" numOctaves="4" stitchTiles="stitch"`. Include `feColorMatrix type="saturate" values="0"` and `feBlend mode="overlay"` and `feComposite operator="in"` as specified in 02-RESEARCH.md Pattern 2. Include `color-interpolation-filters="sRGB"` on the filter element.

    2. **Nav inject div** — `<div id="site-nav"></div>` unchanged.

    3. **`<main id="main-content">`** containing two sections:

       **Hero section:**
       ```html
       <section class="hero" aria-label="Introduction">
         <div class="hero__inner">
           <p class="hero__statement">
             I design game systems that encourage players to reflect on their
             well-being and how they navigate pressure and meaning.
           </p>
           <div class="hero__identity">
             <span class="hero__name">Rebecca Anisman</span>
             <span class="hero__role">Narrative &amp; Experience Engineer</span>
           </div>
         </div>
       </section>
       ```
       No scroll CTA, no decorative rule after it. Hero flows directly into cards via whitespace only.

       **Cards section:**
       ```html
       <section class="cards-section" aria-label="Featured projects">
         <ul class="cards-grid" role="list">
           <li>
             <a class="project-card" href="/work/wellbeing-game/">
               <div class="project-card__thumb thumb-grain" aria-hidden="true"></div>
               <div class="project-card__body">
                 <h2 class="project-card__title">Wellbeing Game</h2>
                 <p class="project-card__descriptor">Narrative systems for player reflection on pressure and meaning.</p>
               </div>
             </a>
           </li>
           <li>
             <a class="project-card" href="/work/experience-design/">
               <div class="project-card__thumb thumb-grain" aria-hidden="true"></div>
               <div class="project-card__body">
                 <h2 class="project-card__title">Experience Design</h2>
                 <p class="project-card__descriptor">Systemic design for emotional resonance and player agency.</p>
               </div>
             </a>
           </li>
           <li>
             <a class="project-card" href="/work/narrative-systems/">
               <div class="project-card__thumb thumb-grain" aria-hidden="true"></div>
               <div class="project-card__body">
                 <h2 class="project-card__title">Narrative Systems</h2>
                 <p class="project-card__descriptor">Writing and design intertwined — games that ask hard, quiet questions.</p>
               </div>
             </a>
           </li>
         </ul>
       </section>
       ```

       NOTE: These are Phase 2 placeholder titles and descriptors. Phase 4 replaces them with real project content. The href slugs use descriptive paths (not `href="#"`) to establish the expected URL structure. The `<h2>` inside each card is correct — these are the primary headings of their card sections; the page-level `<h1>` is omitted because the philosophy statement functions as the primary page identity. If semantic correctness requires an `<h1>`, add a visually hidden one: `<h1 class="visually-hidden">Rebecca Anisman — Narrative &amp; Experience Engineer</h1>` before the hero section.

    4. **Footer inject div** — `<div id="site-footer"></div>` unchanged.

    5. **GSAP scripts + nav.js + animations.js** — copy exactly from the current index.html. Do not change any `<script>` tags.

    After writing, verify the file is valid HTML: `<body>` opens once, `<main>` contains the two sections, `</main>` closes before the footer div, `</body>` closes last.
  </action>
  <verify>
    <automated>grep -c "hero__statement\|cards-grid\|project-card\|grain" "C:/Users/rebec/Documents/Portfolio/index.html"</automated>
    <manual>Open index.html in a text editor and confirm: (1) SVG grain element is first child of body, (2) hero section contains the philosophy text, (3) exactly 3 li > a.project-card elements exist, (4) each card href is /work/[slug]/ not #</manual>
    <sampling_rate>after this task completes</sampling_rate>
  </verify>
  <done>index.html has hidden SVG grain filter, hero section with philosophy + identity, and exactly 3 project card li elements with /work/slug/ hrefs. The placeholder h1 or visually-hidden h1 is present. No splash screens or animation gates on critical content.</done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "nav-height" css/tokens.css` returns `--nav-height: 72px;`
- `grep -c "project-card" index.html` returns at least 9 (3 cards × 3 occurrences per card)
- `grep "href=\"/work/" index.html` returns 3 lines (3 distinct project slugs)
- `grep "hero__statement" index.html` returns 1 line containing the philosophy text
- `grep "grain" index.html` returns lines for both the SVG element and the .thumb-grain divs
- `grep "display:none\|display: none" index.html` returns 0 for the SVG element (must NOT use display:none)
</verification>

<success_criteria>
- tokens.css has `--nav-height: 72px` in :root
- index.html has complete hero + cards structure replacing the Phase 1 placeholder
- 3 project cards each with unique /work/slug/ hrefs
- SVG grain filter defined with correct attributes (no display:none)
- No JS-gated content — all text is in static HTML
- Page commits cleanly to git
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-landing/02-01-SUMMARY.md` using the summary template.
</output>
