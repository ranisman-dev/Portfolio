---
phase: 02-home-landing
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified: []
autonomous: false
requirements:
  - REQ-H01
  - REQ-H02
  - REQ-H03
  - REQ-H04
  - REQ-H05
  - REQ-H06
  - REQ-N02
  - REQ-N04

must_haves:
  truths:
    - "Philosophy statement is readable without scrolling on a 1280px desktop viewport"
    - "Three project cards are visible below the hero in a horizontal row on desktop"
    - "At 375px mobile width, cards stack vertically with no overflow or clipped text"
    - "Hovering a card shows: slight upward lift, deeper shadow, and the dog-ear corner fold deepens"
    - "The footer is visible on the page with working email, LinkedIn, and Resume PDF links"
    - "Page loads without JavaScript errors in the browser console"
  artifacts:
    - path: "index.html"
      provides: "Complete home page visible in browser"
    - path: "css/components.css"
      provides: "Visual styles applied correctly to hero and cards"
  key_links:
    - from: "index.html (SVG #grain filter)"
      to: "css/components.css (.hero::before filter: url(#grain))"
      via: "SVG id attribute matches CSS url() reference"
      pattern: "id=\"grain\""
---

<objective>
Human visual verification of the complete Phase 2 home page — confirm the above-fold hero, responsive card grid, hover interactions, footer links, and mobile layout all behave as specified before marking the phase complete.

Purpose: The quality gate. Plans 01 and 02 produce static files; this checkpoint confirms they work together as a real, usable web page. Catches grain filter rendering, above-fold clipping, mobile stacking, and hover interaction.
Output: Verified home page ready for Phase 3 (which builds on this card structure and URL patterns).
</objective>

<execution_context>
@C:/Users/rebec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rebec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-home-landing/02-CONTEXT.md
@.planning/phases/02-home-landing/02-01-SUMMARY.md
@.planning/phases/02-home-landing/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit home page files and run automated pre-checks</name>
  <files></files>
  <action>
    Commit all Phase 2 changes to git, then run automated pre-checks to confirm the implementation is correct before handing off to human verification.

    **Commit command:**
    ```
    git add index.html css/tokens.css css/components.css
    git commit -m "feat(02): implement home page — hero, cards, grain texture"
    ```

    **Pre-verification automated checks:**

    1. Confirm the SVG filter id matches the CSS reference:
       ```
       grep "id=\"grain\"" index.html
       grep "url(#grain)" css/components.css
       ```
       Both must return results. If either returns nothing, the grain texture won't render — fix before continuing.

    2. Confirm clip-path is on .thumb-grain selector only (not .project-card):
       ```
       grep -n "clip-path" css/components.css
       ```
       All clip-path lines must be in a .thumb-grain selector context. If .project-card { block contains clip-path, it will override border-radius — fix before continuing.

    3. Confirm exactly 3 card hrefs are unique /work/slug/ paths:
       ```
       grep "href=\"/work/" index.html
       ```
       Must return exactly 3 lines with distinct slugs.

    4. Confirm no display:none on the SVG grain element:
       ```
       grep -A3 "id=\"grain\"" index.html
       ```
       Must NOT contain display:none — display:none prevents browsers from processing SVG filters.

    If any automated check fails: fix the issue in the appropriate file, re-stage, re-commit, then surface the issue in the checkpoint task below.

    Local dev reminder: fetch() in nav.js requires HTTP — open index.html via VS Code Live Server (not directly as a file://). The port is typically http://127.0.0.1:5500 or http://localhost:5500.
  </action>
  <verify>
    <automated>grep -c "href=\"/work/" "C:/Users/rebec/Documents/Portfolio/index.html" && grep "url(#grain)" "C:/Users/rebec/Documents/Portfolio/css/components.css" && grep "id=\"grain\"" "C:/Users/rebec/Documents/Portfolio/index.html"</automated>
    <sampling_rate>before handing off to human verify</sampling_rate>
  </verify>
  <done>All automated pre-checks pass. Files committed to git. User is ready to open VS Code Live Server for visual verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — hero, cards, mobile, footer</name>
  <files></files>
  <action>
    Pause for human visual verification of the complete Phase 2 home page in VS Code Live Server. See how-to-verify for exact steps.
  </action>
  <verify>
    <automated>MISSING — visual and interactive behavior requires human verification in a live browser</automated>
    <manual>
      Open VS Code Live Server at http://127.0.0.1:5500

      Step 1 — Above-fold check (REQ-H01):
      Set browser window to 1280px wide. Without scrolling, confirm the full philosophy statement ("I design game systems that encourage players…") AND the name AND the role are all visible. If any are cut off, the hero min-height may need adjustment.

      Step 2 — Grain texture (visual quality):
      Hero background should have a very subtle warm paper texture — slightly warm noise, not a solid flat background. If flat, check browser console for SVG filter errors.

      Step 3 — Cards visible below hero (REQ-H03):
      Scrolling down, 3 cards appear in a horizontal row. Each card shows a crosshatch thumbnail area + title + descriptor. Transition from hero to cards is whitespace only — no visible rule between them.

      Step 4 — Hover interaction:
      Hover each card. Expect: card lifts 4px, shadow deepens, bottom-right thumbnail corner shows a more pronounced dog-ear cut. Transition is smooth ~300ms. If card corners are square despite expected border-radius, dog-ear may be on wrong element.

      Step 5 — Footer (REQ-N02):
      Scroll to bottom. Footer shows: nav echo links, then email link, LinkedIn link, Resume PDF link. Footer is separated by thin rule. Click email — should open mail client. Resume PDF link to /assets/pdfs/resume.pdf may 404 (acceptable until Phase 7).

      Step 6 — Mobile at 375px (REQ-N04):
      DevTools → responsive → 375px. Confirm: no horizontal scrolling, cards stack single column, philosophy statement readable, name and role visible, no clipped elements.

      Step 7 — Browser console:
      Zero errors. typeof gsap returns 'object'.
    </manual>
  </verify>
  <what-built>
    Complete Phase 2 home page:
    - Hero with philosophy statement (italic Montserrat Alternates) + name/role below it
    - CSS paper grain texture on the hero background (SVG feTurbulence)
    - 3 project cards in a horizontal row, each with crosshatch texture thumbnail + title + descriptor
    - Card hover: lift + shadow deepens + dog-ear corner fold deepens
    - Responsive: cards stack to single column at 375px
    - Footer (injected from _includes/footer.html): email / LinkedIn / Resume PDF links
  </what-built>
  <how-to-verify>
    Open VS Code Live Server — typically http://127.0.0.1:5500 — and follow the 7 steps in the verify block above.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 7 steps pass.
    Or describe issues found — for example: "grain not visible", "philosophy cut off on 1280px", "cards not stacking at 375px", "hover not working", "footer missing". Claude will diagnose and fix before you re-verify.
  </resume-signal>
  <done>Human verification passes all 7 steps: above-fold hero, grain texture, cards in row, hover interaction, footer links, mobile 375px, zero console errors.</done>
</task>

</tasks>

<verification>
Phase 2 complete when human verification passes all 7 steps:
1. Philosophy statement + name + role above fold at 1280px (REQ-H01, REQ-H02)
2. Grain texture visible on hero background
3. 3 cards in horizontal row below hero (REQ-H03)
4. Card hover: lift + shadow + dog-ear deepens (REQ-H05 — no animation gate on content)
5. Footer with email link, LinkedIn link, Resume PDF link (REQ-N02)
6. Mobile 375px: single-column cards, no overflow (REQ-N04)
7. Zero console errors, typeof gsap === 'object'
</verification>

<success_criteria>
- Philosophy statement visible above fold at 1280px desktop without scrolling (REQ-H01)
- Name and role visible in hero (REQ-H02)
- 3 project cards in horizontal row, each linking to /work/slug/ (REQ-H03, REQ-H04)
- No content gated by animation or splash screens (REQ-H05)
- Page loads without console errors (REQ-H06 — no external images, performance inherently met)
- Cards stack to single column at 375px mobile (REQ-N04)
- Footer accessible with email + LinkedIn + resume PDF links (REQ-N02)
- Human verification checkpoint passed and documented
- Phase 2 marked complete in STATE.md
</success_criteria>

<output>
After checkpoint passes:
1. Create `.planning/phases/02-home-landing/02-03-SUMMARY.md` using the summary template
2. Update `.planning/STATE.md`: Phase 2 complete, note any style adjustments made during verification
3. Update `.planning/ROADMAP.md`: Phase 2 plan checkboxes marked complete
</output>
