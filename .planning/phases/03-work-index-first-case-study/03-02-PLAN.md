---
phase: 03-work-index-first-case-study
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - css/components.css
  - css/layout.css
autonomous: true
requirements:
  - REQ-W01
  - REQ-W02
  - REQ-W03
  - REQ-W04
  - REQ-W06
  - REQ-W08

must_haves:
  truths:
    - "Work cards display as horizontal row (thumbnail left, text right) with title, role, platform tag, and descriptor visible"
    - "Case study page renders as a centered 65ch reading column with section labels and dividers between each section"
    - "Philosophy callout renders with terracotta left border and italic type — visually matches the home page hero statement"
    - "Section labels render as small-caps monospace in terracotta — distinct from body text, game-doc aesthetic"
    - "Section dividers render as single peach diamond glyph — quiet breath mark, not a hard rule"
    - "Case study nav footer shows back-to-work link and prev/next project links"
    - "Work cards: hover lifts 2px, border-left snaps to terracotta, title shifts to terracotta, shadow fades in via ::after opacity"
  artifacts:
    - path: "css/components.css"
      provides: ".page-header, .work-list, .work-card (all variants), .case-study, .case-study__section, .case-study__label, .case-study__hero, .case-study__meta, .case-study__tag, .case-study__hero-image, .section-divider, .philosophy-callout, .artifact-brief, .case-study-nav"
    - path: "css/layout.css"
      provides: "reading column layout for .case-study (if not on components.css)"
  key_links:
    - from: "css/components.css (.work-card)"
      to: "css/tokens.css (--color-accent-subtle, --color-accent, --duration-base)"
      via: "CSS custom property references"
      pattern: "var\\(--color-accent"
    - from: "css/components.css (.philosophy-callout)"
      to: "css/components.css (.hero__statement)"
      via: "Same border-left + italic + font-display visual DNA"
      pattern: "border-left: 3px solid var\\(--color-accent\\)"
    - from: "css/components.css (.case-study)"
      to: "css/tokens.css (--max-width-prose)"
      via: "max-width: var(--max-width-prose)"
      pattern: "max-width-prose"
---

<objective>
Add all CSS component styles for Phase 3: Work Index card variant, case study reading column layout, section labels, section dividers, philosophy callout, page header, and case study nav footer. All styles extend components.css and use existing design tokens — no new tokens needed.

Purpose: The visual layer that makes the 8-section template readable and on-brand. These styles are the Phase 4 template's visual DNA — getting them right here means Phase 4 is pure content, not CSS debugging.

Output: New sections appended to css/components.css covering all Phase 3 component classes. Minor layout.css addition if the .case-study reading column warrants it there.
</objective>

<execution_context>
@C:/Users/rebec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rebec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-work-index-first-case-study/03-CONTEXT.md
@.planning/phases/03-work-index-first-case-study/03-RESEARCH.md

<interfaces>
<!-- Key tokens to use. Extracted from css/tokens.css. -->

Colors:
  --color-bg: #fdf8f0
  --color-bg-surface: #f5ede0
  --color-text: #2a1f14
  --color-text-muted: #7a6552
  --color-accent: #c4614a          (terracotta — labels, hover states, borders)
  --color-accent-hover: #a84d39
  --color-accent-subtle: #f0d9d3   (very light tint — card border at rest, tag bg)
  --color-secondary: #f2d5aa       (peach — section divider glyph)
  --color-border: #d9cdc0

Type:
  --font-display: 'Montserrat Alternates', sans-serif
  --font-body: 'Source Code Pro', monospace    (body IS monospace — game-doc feel)
  --text-xs: 0.563rem    (labels, captions)
  --text-sm: 0.75rem     (secondary text)
  --text-base: 1rem      (body copy)
  --text-md: 1.333rem    (card titles on work index)
  --leading-normal: 1.65
  --leading-snug: 1.4
  --leading-loose: 1.85
  --weight-normal: 400
  --weight-medium: 500
  --weight-semibold: 600

Spacing:
  --space-2: 0.5rem   --space-3: 0.75rem   --space-4: 1rem
  --space-5: 1.25rem  --space-6: 1.5rem    --space-8: 2rem
  --space-10: 2.5rem  --space-12: 3rem     --space-16: 4rem
  --space-20: 5rem

Layout:
  --max-width-prose: 65ch    (case study reading column — already in 60-70ch window)

Animation:
  --duration-base: 300ms
  --ease-out: cubic-bezier(0.0, 0.0, 0.2, 1)

Radius:
  --radius-sm: 4px    --radius-base: 8px

<!-- Phase 2 established patterns — replicate exactly, do not hand-roll -->

.project-card hover shadow pattern (replicate for .work-card):
  - NO direct box-shadow transition — use ::after opacity (GPU composited, no repaint)
  - ::after has position:absolute, inset:0, box-shadow, opacity:0, transition on opacity
  - :hover sets ::after opacity:1
  - transform: translateY(-4px) on .project-card — use translateY(-2px) for .work-card (subtler)

.hero__statement visual DNA (replicate for .philosophy-callout):
  - border-left: 3px solid var(--color-accent)
  - font-style: italic
  - font-family: var(--font-display)
  - max-width: 52ch

Existing CSS structure (append new sections to END of css/components.css):
  - Do not modify any existing rules
  - Add new sections with /* ── Section Name ──... */ comment headers
  - Follow existing naming conventions: BEM-style, semantic names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work Index CSS — page header and work card styles</name>
  <files>css/components.css</files>
  <action>
    Append two new sections to the END of css/components.css. Do not modify any existing rules.

    Section A: Page Header (reusable for Writing, About, etc. later)
    ```css
    /* ── Page Header ──────────────────────────────────── */
    /* Minimal page-level header: used on Work, Writing, About.
       Just a title with a bottom border — no intro paragraph. */

    .page-header {
      padding: var(--space-12) 0 var(--space-8);
      border-bottom: 1px solid var(--color-border);
      margin-bottom: var(--space-10);
    }

    .page-header__title {
      font-family: var(--font-display);
      font-size: var(--text-2xl);
      font-weight: var(--weight-semibold);
      color: var(--color-text);
      line-height: var(--leading-tight);
      margin: 0;
    }
    ```

    Section B: Work Index Card List
    Use the full CSS from RESEARCH.md Pattern 1 and the CSS block in the "Work Index Card Variant" section verbatim. Key details:
    - `.work-list`: display:flex, flex-direction:column, gap:var(--space-4), list-style:none, padding:0, margin:0
    - `.work-card`: display:flex, flex-direction:row, text-decoration:none, color:inherit, background-color:var(--color-bg-surface), border:none, border-left:3px solid var(--color-accent-subtle), border-radius:var(--radius-base), overflow:hidden, position:relative, box-shadow:none, transform:translateY(0), transition on transform and border-left-color
    - `.work-card::after`: shadow via opacity pattern — content:"", position:absolute, inset:0, border-radius:inherit, box-shadow:0 16px 20px -8px rgba(42,31,20,0.18), opacity:0, transition on opacity, pointer-events:none
    - `.work-card:hover, .work-card:focus-within`: transform:translateY(-2px), border-left-color:var(--color-accent)
    - `.work-card:hover::after, .work-card:focus-within::after`: opacity:1
    - `.work-card:hover .work-card__title, .work-card:focus-within .work-card__title`: color:var(--color-accent)
    - `.work-card__thumb`: width:120px, flex-shrink:0, aspect-ratio:unset (override thumb-grain's 16/9 to fill card height), align-self:stretch
    - `.work-card__body`: padding:var(--space-5) var(--space-6), display:flex, flex-direction:column, gap:var(--space-2)
    - `.work-card__title`: font-family:var(--font-display), font-size:var(--text-md), font-weight:var(--weight-semibold), color:var(--color-text), margin:0, line-height:var(--leading-snug), transition on color
    - `.work-card__meta`: display:flex, gap:var(--space-3), align-items:center, flex-wrap:wrap, margin:0
    - `.work-card__role`: font-family:var(--font-body), font-size:var(--text-sm), color:var(--color-text-muted)
    - `.work-card__tag`: font-family:var(--font-body), font-size:var(--text-xs), color:var(--color-accent), background-color:var(--color-accent-subtle), padding:2px var(--space-2), border-radius:var(--radius-sm), text-transform:uppercase, letter-spacing:0.06em, white-space:nowrap
    - `.work-card__descriptor`: font-family:var(--font-body), font-size:var(--text-sm), color:var(--color-text-muted), margin:0, line-height:var(--leading-normal)
    - Mobile breakpoint `@media (max-width: 480px)`: .work-card flex-direction:column, .work-card__thumb width:100% and aspect-ratio:16/9 (restore standard ratio when stacked)
    - Reduced motion: `@media (prefers-reduced-motion: reduce)` — .work-card and .work-card::after transition to opacity + border-color only, .work-card:hover transform:none
  </action>
  <verify>Open work/index.html in browser via Live Server. Confirm: (1) "Work" heading has bottom border, (2) project-1 card displays as horizontal row (thumbnail left, text right), (3) platform/status tag has peach background and terracotta text, (4) hover lifts card 2px, border-left snaps to terracotta, title turns terracotta, shadow fades in. No console errors.</verify>
  <done>.page-header and .page-header__title style the Work heading correctly. .work-card renders as horizontal row with correct metadata, tag, and hover interactions. Mobile: card stacks at 480px breakpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Case study CSS — reading column, sections, dividers, philosophy callout, nav footer</name>
  <files>css/components.css</files>
  <action>
    Append additional sections to the END of css/components.css (after Task 1's additions). Do not modify any existing rules.

    Section C: Case Study Layout and Sections
    ```css
    /* ── Case Study Layout ────────────────────────────── */
    /* Reading column: 65ch centered — in the 60-70ch target window.
       This is the canonical layout for all project pages.
       Phase 4 copies this template verbatim — do not change without updating project-2/3 stubs. */

    .case-study {
      max-width: var(--max-width-prose);     /* 65ch */
      margin-inline: auto;
      padding: var(--space-16) var(--space-6) var(--space-20);
    }

    .case-study__section {
      /* Each section is a distinct content block. Spacing handled by section-divider above. */
    }

    /* Section 1: Hero — title, metadata, and hero image */
    .case-study__hero {
      padding-bottom: var(--space-4);
    }

    .case-study__title {
      font-family: var(--font-display);
      font-size: var(--text-xl);
      font-weight: var(--weight-semibold);
      color: var(--color-text);
      line-height: var(--leading-tight);
      margin: 0 0 var(--space-4);
    }

    .case-study__meta {
      display: flex;
      gap: var(--space-3);
      align-items: center;
      flex-wrap: wrap;
      margin-bottom: var(--space-8);
    }

    .case-study__role {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text-muted);
    }

    /* Platform/status tag — same visual pattern as .work-card__tag */
    .case-study__tag {
      font-family: var(--font-body);
      font-size: var(--text-xs);
      color: var(--color-accent);
      background-color: var(--color-accent-subtle);
      padding: 2px var(--space-2);
      border-radius: var(--radius-sm);
      text-transform: uppercase;
      letter-spacing: 0.06em;
      white-space: nowrap;
    }

    /* Hero image slot — thumb-grain fills this; override aspect-ratio for better case study proportion */
    .case-study__hero-image {
      aspect-ratio: 16 / 9;      /* override from thumb-grain if desired — adjust per project */
      border-radius: var(--radius-base);
    }
    ```

    Section D: Section Label (game-doc aesthetic)
    ```css
    /* ── Case Study Section Label ─────────────────────── */
    /* Small-caps monospace label above each section.
       Source Code Pro gives the game-design-document feel.
       Terracotta color: visible but not dominant. */

    .case-study__label {
      font-family: var(--font-body);           /* Source Code Pro — monospace = game-doc */
      font-size: var(--text-xs);               /* Small — this is a label, not a heading */
      font-weight: var(--weight-semibold);
      text-transform: uppercase;
      letter-spacing: 0.12em;
      color: var(--color-accent);
      margin-bottom: var(--space-3);
      /* No margin-top — section-divider handles the gap above */
    }
    ```

    Section E: Section Divider (origami accent mark)
    ```css
    /* ── Section Divider ──────────────────────────────── */
    /* Single peach diamond between case study sections.
       Echoes the home page diamond geometry — quiet breath mark, not a hard rule.
       NOT the repeating diamond strip (.cards-section::before) — that's for the home page.
       Peach (--color-secondary) is softer than terracotta — reads as pause, not stop. */

    .section-divider {
      width: 100%;
      margin: var(--space-10) 0;
      text-align: center;
      position: relative;
    }

    .section-divider::before {
      content: "◆";
      font-size: 0.5rem;
      color: var(--color-secondary);
      opacity: 0.8;
    }
    ```

    Section F: Philosophy Callout (REQ-W04)
    ```css
    /* ── Philosophy Callout ───────────────────────────── */
    /* 1-3 sentences connecting the project to the design throughline
       (well-being, pressure, meaning). Required on every project page (REQ-W04).
       Reuses the .hero__statement visual DNA: terracotta left border + italic + Montserrat.
       This visual connection is intentional — callout echoes the hero's voice. */

    .philosophy-callout {
      border-left: 3px solid var(--color-accent);
      padding-left: var(--space-6);
      margin: var(--space-8) 0;
      font-style: italic;
      color: var(--color-text);
      font-family: var(--font-display);
      font-size: var(--text-base);
      line-height: var(--leading-loose);
      max-width: 52ch;
    }

    .philosophy-callout p {
      margin: 0;
    }
    ```

    Section G: Artifact Brief (REQ-W06 — shown only when prototype adds information)
    ```css
    /* ── Artifact Brief ───────────────────────────────── */
    /* Used ONLY when a prototype/artifact adds information screenshots cannot convey.
       REQ-W06: each artifact accompanied by 2-4 sentence brief (design question, what to notice, discovery).
       In HTML, this block is commented out by default — uncomment only when a real artifact is present. */

    .artifact-brief {
      background-color: var(--color-bg-inset);
      border-radius: var(--radius-base);
      padding: var(--space-6);
      margin: var(--space-8) 0;
    }

    .artifact-brief p {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      line-height: var(--leading-normal);
      margin: 0 0 var(--space-4);
    }

    .artifact-brief p:last-of-type {
      margin-bottom: 0;
    }
    ```

    Section H: Case Study Nav Footer (REQ-W08)
    ```css
    /* ── Case Study Nav Footer ───────────────────────── */
    /* Prev/next project navigation + back-to-work link.
       Required on every project page (REQ-W08).
       MAINTENANCE: if you change project order, update prev/next hrefs in all project files. */

    .case-study-nav {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      gap: var(--space-4);
      padding: var(--space-8) 0 var(--space-4);
      border-top: 1px solid var(--color-border);
      margin-top: var(--space-10);
    }

    .case-study-nav__back {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      text-decoration: none;
    }

    .case-study-nav__back:hover,
    .case-study-nav__back:focus {
      color: var(--color-accent);
    }

    .case-study-nav__siblings {
      display: flex;
      gap: var(--space-6);
    }

    .case-study-nav__prev,
    .case-study-nav__next {
      font-family: var(--font-body);
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      text-decoration: none;
    }

    .case-study-nav__prev:hover,
    .case-study-nav__prev:focus,
    .case-study-nav__next:hover,
    .case-study-nav__next:focus {
      color: var(--color-accent);
    }

    /* Mobile: stack back link above prev/next */
    @media (max-width: 480px) {
      .case-study-nav {
        flex-direction: column;
        align-items: flex-start;
      }
    }
    ```
  </action>
  <verify>Open work/project-1/index.html in browser via Live Server. Confirm: (1) page content is a centered 65ch column, (2) section labels render as small terracotta uppercase monospace text above each section, (3) single peach diamond appears between sections, (4) philosophy callout in Reflection section has terracotta left border and italic Montserrat type, (5) case-study-nav footer appears at bottom with back link and prev/next, (6) no console errors.</verify>
  <done>All Phase 3 CSS components styled. Case study reads as a narrow centered column with clear visual hierarchy: terracotta labels, peach diamond dividers, philosophy callout echoing the hero statement, and nav footer with hover states.</done>
</task>

</tasks>

<verification>
- css/components.css has new sections: .page-header, .work-list/.work-card (all variants + hover + mobile + reduced-motion), .case-study (all variants), .case-study__label, .section-divider, .philosophy-callout, .artifact-brief, .case-study-nav (all variants + mobile)
- No existing CSS rules modified — only appended
- All values use CSS custom properties from tokens.css — no hardcoded hex colors or px values except where tokens don't exist
- Work card hover shadow uses ::after opacity pattern (no direct box-shadow transition)
- Philosophy callout matches hero statement visual DNA (border-left + italic + font-display)
</verification>

<success_criteria>
- Work Index card renders as horizontal row with correct hover interactions
- Case study page renders as centered 65ch reading column
- Section labels are visually distinct (small-caps monospace terracotta)
- Section dividers are single peach diamond — quiet, not dominant
- Philosophy callout is visually connected to home page hero statement
- Case study nav footer present with hover states
- Mobile breakpoints work at 480px for both work cards and case study nav
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-index-first-case-study/03-02-SUMMARY.md`
</output>
