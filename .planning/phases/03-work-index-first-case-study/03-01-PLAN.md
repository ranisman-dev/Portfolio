---
phase: 03-work-index-first-case-study
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - work/index.html
  - work/project-1/index.html
  - work/project-2/index.html
  - work/project-3/index.html
  - js/nav.js
  - index.html
autonomous: true
requirements:
  - REQ-W01
  - REQ-W02
  - REQ-W03
  - REQ-W04
  - REQ-W05
  - REQ-W06
  - REQ-W07
  - REQ-W08

must_haves:
  truths:
    - "Visiting /work/ shows a single-column list of work cards with one visible card and two commented-out stubs"
    - "Visiting /work/project-1/ shows a complete 8-section case study page with all sections present and labeled"
    - "Visiting /work/project-2/ and /work/project-3/ shows stub pages — accessible but clearly placeholder"
    - "The Work nav link highlights as active when on /work/ and on any /work/project-*/ sub-page"
    - "Home page card hrefs point to /work/project-1/ — no 404 on click"
  artifacts:
    - path: "work/index.html"
      provides: "Work Index — single-column card list, one visible card, two commented-out stubs, maintainability comment block"
    - path: "work/project-1/index.html"
      provides: "Case study template — full 8-section article with placeholder content, philosophy callout, nav footer"
    - path: "work/project-2/index.html"
      provides: "Stub page — 8-section shell with bracketed placeholder content"
    - path: "work/project-3/index.html"
      provides: "Stub page — 8-section shell with bracketed placeholder content"
    - path: "js/nav.js"
      provides: "Active nav state for section sub-pages via startsWith matching"
  key_links:
    - from: "work/index.html"
      to: "work/project-1/index.html"
      via: "work-card href=/work/project-1/"
      pattern: "href=\"/work/project-1/\""
    - from: "work/project-1/index.html"
      to: "/work/"
      via: "case-study-nav back link"
      pattern: "href=\"/work/\""
    - from: "index.html"
      to: "work/project-1/index.html"
      via: "home page featured card href"
      pattern: "href=\"/work/project-1/\""
    - from: "js/nav.js"
      to: "_includes/nav.html"
      via: "navReady event + startsWith pathname matching"
      pattern: "startsWith"
---

<objective>
Build the Work Index page and the full 8-section case study template, create directory structure for three projects, fix home page broken hrefs, and extend nav.js to activate the Work nav link on all /work/ sub-pages.

Purpose: The Work Index establishes the site's core credibility engine. The project-1 case study template is the canonical pattern Phase 4 replicates — getting the 8-section structure right here is the primary quality gate for this phase.

Output: work/index.html (full card list), work/project-1/index.html (8-section template), work/project-2/ and work/project-3/ (stub shells), updated js/nav.js (active state for sub-pages), updated index.html (fixed hrefs).
</objective>

<execution_context>
@C:/Users/rebec/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rebec/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-index-first-case-study/03-CONTEXT.md
@.planning/phases/03-work-index-first-case-study/03-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase. Use these directly — no codebase exploration needed. -->

From index.html (home page):
- SVG grain filter: position:absolute;overflow:hidden wrapper — NEVER display:none
- Nav/footer: <div id="site-nav"></div> and <div id="site-footer"></div> — no inline markup
- Script load order: GSAP core → ScrollTrigger → nav.js → animations.js, all with defer
- All CSS: /css/tokens.css → /css/base.css → /css/layout.css → /css/components.css → /css/animations.css

From js/nav.js (current active-state logic — exact match only):
```javascript
// Current: exact match only — does NOT activate Work link on /work/project-1/
if (linkPath === pagePath) {
  link.setAttribute('aria-current', 'page');
  link.classList.add('is-active');
}
```
Must extend to use startsWith so /work/project-1/ activates the /work/ nav link.

From css/tokens.css (key tokens for HTML authors):
- --color-accent: #c4614a (terracotta)
- --color-accent-subtle: #f0d9d3
- --color-secondary: #f2d5aa (peach)
- --color-text: #2a1f14
- --color-text-muted: #7a6552
- --color-bg-surface: #f5ede0
- --color-border: #d9cdc0
- --max-width-prose: 65ch
- --font-display: 'Montserrat Alternates'
- --font-body: 'Source Code Pro' (monospace)

From index.html (home page — BROKEN hrefs that must be fixed):
- Line 61: href="/work/wellbeing-game/" → must become href="/work/project-1/"
- Line 70: href="/work/experience-design/" → must become href="/work/project-2/" (or leave as stub with comment)
- Line 79: href="/work/narrative-systems/" → must become href="/work/project-3/" (or leave as stub with comment)

From css/components.css (.site-nav__link active state — already styled):
- .site-nav__link.is-active, .site-nav__link[aria-current="page"] { color: var(--color-text); font-weight: var(--weight-semibold); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work Index HTML and directory structure</name>
  <files>
    work/index.html
    work/project-1/index.html
    work/project-2/index.html
    work/project-3/index.html
  </files>
  <action>
    Replace work/index.html with the full Work Index page. Replace the existing placeholder content with a single-column card list using the patterns from 03-RESEARCH.md.

    Work/index.html structure:
    - `<body class="page-work">` — same as current stub
    - SVG grain filter block (copy from index.html — same position:absolute;overflow:hidden wrapper pattern; required so thumb-grain fold works)
    - `<div id="site-nav"></div>`
    - `<main id="main-content"><div class="container">`
    - `<header class="page-header"><h1 class="page-header__title">Work</h1></header>`
    - Maintainability comment block immediately before `<ul class="work-list" role="list">`:
      ```html
      <!-- Work card list — one card per project, single column.
           To show a project: uncomment its <li> block below.
           To add a 4th project: create /work/project-4/index.html (copy project-1 template),
           then add a new <li> block following the same structure as project-1's card. -->
      ```
    - ONE visible `<li>` for project-1 using `.work-card` structure (see RESEARCH.md Pattern 1):
      ```html
      <li>
        <a class="work-card" href="/work/project-1/">
          <div class="work-card__thumb thumb-grain" aria-hidden="true"></div>
          <div class="work-card__body">
            <h2 class="work-card__title">[Project Title]</h2>
            <p class="work-card__meta">
              <span class="work-card__role">[Specific Role]</span>
              <span class="work-card__tag">[Platform / Status]</span>
            </p>
            <p class="work-card__descriptor">[One evocative sentence describing this project.]</p>
          </div>
        </a>
      </li>
      ```
    - TWO commented-out `<li>` blocks for project-2 and project-3 (HTML comments, NOT CSS display:none):
      Each stub card uses `[Project 2 — placeholder]` and `[Project 3 — placeholder]` as title.
    - `<div id="site-footer"></div>`
    - Scripts: GSAP core, ScrollTrigger, /js/nav.js, /js/animations.js — all with defer, root-relative paths

    Create work/project-1/index.html — the canonical 8-section case study template:
    - `<head>`: title "Project Title — Origami Games", meta description "[Project summary]", same CSS/font links as work/index.html. All paths root-relative (/css/tokens.css etc.).
    - `<body class="page-case-study">`
    - SVG grain filter block (same as above — required for thumb-grain)
    - `<div id="site-nav"></div>`
    - `<main id="main-content"><article class="case-study">` (NOT article as outer wrapper replacing main — both are required)
    - 8 sections in order, each preceded by `<div class="section-divider" aria-hidden="true"></div>` (except before section 1):
      1. `.case-study__hero` — contains `.case-study__label` ("Project"), `<h1 class="case-study__title">[Project Title]</h1>`, `.case-study__meta` (role span + tag span), `.case-study__hero-image thumb-grain` with aspect-ratio override comment
      2. `.case-study__section` — label "Summary", `<p>[2–4 sentence project summary for non-industry readers.]</p>`
      3. `.case-study__section` — label "Challenge", `<p>[Design challenge / problem statement.]</p>`
      4. `.case-study__section` — label "Process", `<p>[Process / approach narrative with supporting artifacts.]</p>` PLUS commented-out `.artifact-brief` aside block with REQ-W06 comment explaining when to uncomment
      5. `.case-study__section` — label "Outcome", `<p>[Outcome with qualitative evidence where available.]</p>`
      6. `.case-study__section` — label "Reflection", `<p>[Reflection paragraph.]</p>` PLUS philosophy callout immediately after the paragraph: `<blockquote class="philosophy-callout"><p>[1–3 sentences explicitly connecting this project to well-being, pressure, and meaning.]</p></blockquote>`
      7. `.case-study__section` — label "Writing", `<p>[Attached writing/documents with inline excerpts — cross-linked from /writing/ in Phase 5.]</p>`
      8. `<nav class="case-study-nav" aria-label="Project navigation">` containing: `.case-study-nav__back` linking to /work/, `.case-study-nav__siblings` div containing `.case-study-nav__prev` (href="/work/project-3/", aria-label="Previous project") and `.case-study-nav__next` (href="/work/project-2/", aria-label="Next project"). Add HTML comment: `<!-- MAINTENANCE: if you change project order, update prev/next hrefs in all project files -->`
    - Close `</article></main>`
    - `<div id="site-footer"></div>`
    - Same script block as work/index.html

    Create work/project-2/index.html and work/project-3/index.html — identical stub shells:
    - Same structure as project-1 but with titles "[Project 2 — placeholder]" and "[Project 3 — placeholder]"
    - Same 8 sections with bracketed placeholders throughout
    - Title in `<head>`: "Project 2 — Origami Games" / "Project 3 — Origami Games"
    - Prev/next in case-study-nav should be placeholder hrefs pointing at the adjacent stubs
    - Include comment at top of `<body>`: `<!-- STUB: This page is not yet visible in the Work index. Uncomment this project's card in /work/index.html to make it visible. -->`

    CRITICAL rules (apply everywhere):
    - All paths root-relative: /css/tokens.css, /js/nav.js, /work/project-1/ — never relative paths
    - SVG grain filter: position:absolute;overflow:hidden — NEVER display:none
    - Nav/footer: div#site-nav and div#site-footer only — no inline markup
    - Do NOT apply clip-path to .work-card wrapper — only to .thumb-grain (Phase 2 rule)
    - Stub cards in work/index.html: commented out in HTML, never with CSS display:none
  </action>
  <verify>Open work/index.html in browser via Live Server. Confirm: (1) page loads at /work/ with heading "Work", (2) one card is visible, (3) source shows two commented-out li blocks, (4) clicking the card navigates to /work/project-1/, (5) project-1 shows 8 labeled sections including philosophy callout and case-study-nav footer. Open /work/project-2/ and /work/project-3/ — both load without 404.</verify>
  <done>work/index.html has full card list structure with one visible card and two commented-out stubs. work/project-1/index.html has complete 8-section article with all sections labeled, philosophy callout in Reflection, and case-study-nav footer. project-2 and project-3 stub pages exist and are accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix home page hrefs and extend nav.js active state</name>
  <files>
    index.html
    js/nav.js
  </files>
  <action>
    Fix index.html home page card hrefs — the three featured project cards currently link to non-existent URLs:
    - Change href="/work/wellbeing-game/" → href="/work/project-1/"
    - Change href="/work/experience-design/" → href="/work/project-2/"
    - Change href="/work/narrative-systems/" → href="/work/project-3/"
    Do not change any other content on the home page. Keep titles and descriptors as-is.

    Extend js/nav.js to activate the Work nav link on all /work/ sub-pages:
    The current navReady event listener uses exact-match only (`linkPath === pagePath`). This means visiting /work/project-1/ does NOT activate the Work nav link. Replace the active state logic with a startsWith pattern that handles section sub-pages.

    Replace the existing navReady event listener block with this updated version:
    ```javascript
    // Set active state on nav links after nav is injected.
    // Compares window.location.pathname to each link's href attribute.
    // Uses startsWith for section links so sub-pages (e.g. /work/project-1/)
    // activate the parent section link (e.g. /work/).
    // Guard: the root '/' link only matches exactly to avoid matching every page.
    document.addEventListener('navReady', function () {
      var currentPath = window.location.pathname;
      var links = document.querySelectorAll('.site-nav__link');

      links.forEach(function (link) {
        var linkHref = link.getAttribute('href');

        // Normalize: strip trailing slash, but keep root '/' as-is
        var linkPath = linkHref.replace(/\/$/, '') || '/';
        var pagePath = currentPath.replace(/\/$/, '') || '/';

        var isMatch = false;
        if (linkPath === '/') {
          // Root: exact match only — avoids matching every page
          isMatch = (pagePath === '/');
        } else {
          // Section links: match exact OR sub-pages (e.g. /work matches /work/project-1)
          isMatch = (pagePath === linkPath) || currentPath.startsWith(linkHref);
        }

        if (isMatch) {
          link.setAttribute('aria-current', 'page');
          link.classList.add('is-active');
        }
      });
    });
    ```
    Keep all other nav.js content (the IIFE wrapper, injectInclude function, the two injectInclude calls) exactly as-is. Only replace the navReady event listener block.
  </action>
  <verify>In browser via Live Server: (1) visit /work/ — Work nav link is bold/dark, (2) visit /work/project-1/ — Work nav link is still bold/dark (startsWith matching working), (3) visit home page — Home nav link is bold/dark and Work is not, (4) home page three cards no longer 404 (they navigate to /work/project-1/, /work/project-2/, /work/project-3/).</verify>
  <done>index.html featured card hrefs point to real directories. js/nav.js navReady listener uses startsWith matching so Work nav link activates on any /work/* URL. Exact match preserved for root '/' to avoid false matches.</done>
</task>

</tasks>

<verification>
- work/index.html: single-column list with one visible card, two HTML-commented stubs, maintainability comment block
- work/project-1/index.html: complete 8-section article, all sections have case-study__label, section-divider between each, philosophy-callout in Reflection section, case-study-nav footer with prev/next and back-to-work
- work/project-2/index.html and work/project-3/index.html: stub shells load without 404
- All paths root-relative (no ../../ relative paths anywhere)
- index.html home page card hrefs: /work/project-1/, /work/project-2/, /work/project-3/
- js/nav.js: Work nav link activates on /work/ and all /work/* sub-pages
</verification>

<success_criteria>
- /work/ loads with correct structure and one visible card
- /work/project-1/ loads with all 8 sections labeled, philosophy callout present, nav footer functional
- /work/project-2/ and /work/project-3/ load (stubs)
- Home page cards do not 404
- Work nav link highlights correctly on all /work/* pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-index-first-case-study/03-01-SUMMARY.md`
</output>
